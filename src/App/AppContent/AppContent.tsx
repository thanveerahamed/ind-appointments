import Box from '@mui/material/Box';
import Home from '../../components/Home/Home';

const AppContent = () => {
  return (
    <Box sx={{ margin: '10px' }}>
      <Home />
    </Box>
  );
};

export default AppContent;
